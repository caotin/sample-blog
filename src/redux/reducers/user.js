const defaultState = {
  data: null,
  isAuthen: false,
  status: ""
};
function user(state = defaultState, { type, payload }) {
  switch (type) {
    case "case":
      break;

    default:
      return state;
  }
}

export default user;
